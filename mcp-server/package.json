{
  "name": "stl-buildloop-mcp",
  "version": "0.4.0-dev",
  "private": true,
  "type": "module",
  "description": "BuildLoop MCP server: gives a STLFLIX collaborator their own Postgres database on the ops cluster, and nothing else.",
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.21.0",
    "express": "^5.1.0",
    "pg": "^8.16.3"
  },
  "engines": {
    "node": ">=22"
  }
}
